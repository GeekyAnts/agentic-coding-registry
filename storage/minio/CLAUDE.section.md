## Storage: MinIO (S3-compatible)

- S3-compatible object storage for local dev. Console: http://localhost:9001.
- Endpoint `http://localhost:9000`; credentials `minio` / `minio123` (dev only).
- Provided by the `minio` service in docker-compose.

# Docs (Starlight)

Astro + [Starlight](https://starlight.astro.build) documentation site.

```
pnpm install
pnpm dev        # http://localhost:4321
pnpm build      # static site -> dist/
```

Content lives in `src/content/docs/`; configure title/sidebar in `astro.config.mjs`.

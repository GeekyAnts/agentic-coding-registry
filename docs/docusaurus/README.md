# Documentation (Docusaurus)

This is a self-contained Docusaurus site. It has its own `package.json`, so
install and run it from within this folder:

```bash
cd docs
pnpm install
pnpm start      # dev server
pnpm build      # static build → docs/build
```

Write docs as Markdown/MDX files in `docs/`.

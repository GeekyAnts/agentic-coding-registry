## Database: PostgreSQL

- Connect via `DATABASE_URL` (e.g. `postgres://app:app@localhost:5432/app`).
- Local instance provided by the `postgres` service in docker-compose.
- Keep migrations in version control; never commit real credentials.

## Stack: Python + FastAPI

- **Language:** Python. **Framework:** FastAPI. **Server:** uvicorn.
- **Structure:** `main.py` entry, routers in `app/routers/`, models in `app/models/`.
- **Commands:**
  ```
  pip install -r requirements.txt
  uvicorn main:app --reload
  pytest
  ```

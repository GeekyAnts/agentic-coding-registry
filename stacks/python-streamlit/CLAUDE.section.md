## Stack: Python + Streamlit

- **Language:** Python. **Framework:** Streamlit.
- **Structure:** `app.py` entry; pages in `pages/`; helpers in `lib/`.
- **Commands:**
  ```
  pip install -r requirements.txt
  streamlit run app.py
  ```

## Stack: Laravel

- **Language:** PHP. **Framework:** Laravel. **PM:** Composer.
- **Structure:** routes in `routes/`, controllers in `app/Http/Controllers/`,
  models in `app/Models/`, views in `resources/views/`.
- **Commands:**
  ```
  composer install
  php artisan serve
  php artisan migrate
  php artisan test
  ```

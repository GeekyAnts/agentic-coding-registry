import { defineConfig } from "astro/config";

// Static output by default. For SSR, add an adapter (e.g. @astrojs/node,
// @astrojs/vercel, @astrojs/cloudflare) and set `output: "server"`.
export default defineConfig({});

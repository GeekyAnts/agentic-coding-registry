# React + Vite — Setup Notes

Starter guidance for the React + Vite stack. Flesh out as the project grows.

## Getting started
```
pnpm create vite@latest . --template react-ts
pnpm install
pnpm dev
```

## Recommended additions
- ESLint + Prettier
- Vitest + Testing Library
- A router (React Router / TanStack Router)

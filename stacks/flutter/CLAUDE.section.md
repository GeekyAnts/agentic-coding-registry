## Stack: Flutter

- **Language:** Dart. **SDK:** Flutter.
- **Structure:** `lib/` source, `lib/screens/`, `lib/widgets/`, `test/`.
- **Commands:**
  ```
  flutter pub get
  flutter run
  flutter test
  flutter build apk
  ```

# Base skeleton — per-stack jobs are appended by create-ai-native-project.
stages:
  - build
  - test

lint:
  stage: build
  image: alpine
  script:
    - echo "Add repo-wide lint/format checks here"

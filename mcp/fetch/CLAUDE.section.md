## MCP Server: Fetch

Lets the agent fetch a URL and convert the page to clean Markdown for reading —
handy for pulling in docs or references on demand. Runs over stdio via
`uvx mcp-server-fetch`; requires `uv`/Python on the machine.

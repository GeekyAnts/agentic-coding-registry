## MCP Server: GitHub

Connects the agent to GitHub — repositories, issues, pull requests, code search,
and Actions — so it can read and act on your GitHub without leaving the editor.
Uses the official remote HTTP server at `https://api.githubcopilot.com/mcp/` and
authenticates via OAuth on first use (in Claude Code, run `/mcp` to sign in).

## MCP Server: Chrome DevTools

Lets the agent drive a real Chrome instance — inspect the DOM, read console and
network activity, run performance / Lighthouse traces, and take screenshots — so
it can debug and verify frontend changes in a live browser. Runs over stdio via
`npx chrome-devtools-mcp@latest`; no credentials needed.

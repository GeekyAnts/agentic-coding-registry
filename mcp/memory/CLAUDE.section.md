## MCP Server: Memory

A persistent knowledge-graph memory the agent can write to and query across a
session, so useful facts survive between turns. Runs over stdio via
`npx @modelcontextprotocol/server-memory`; no credentials needed.

## MCP Server: Playwright

Lets the agent drive a real browser (Chromium, Firefox, or WebKit) — navigate,
fill forms, click, take screenshots, and run end-to-end checks against your UI.
Runs over stdio via `npx @playwright/mcp@latest`; no credentials needed.
